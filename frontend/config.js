const CONFIG = {
  // Set to empty string to use direct Supabase auth (no backend needed)
  API_URL: '',
  
  // Admin email - this user will have admin privileges
  ADMIN_EMAIL: '2063235@learn.vsb.bc.ca',
  
  // Firebase is now used instead of Supabase
  USE_FIREBASE: true
};

// App is configured (no longer in demo mode)
const SUPABASE_CONFIGURED = true;
